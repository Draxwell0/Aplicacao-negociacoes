export enum DiasDaSemana{
    domingo = 0,
    segunda = 1,
    terca = 2,
    quarta = 3,
    quinta = 4,
    sexta = 5,
    sabado = 6
}

// As enum começam de 0, porém, se modificarmos o valor de alguma das enum,
// os próximos valores passarão a contar a partir do novo valor.